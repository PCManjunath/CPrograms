#include <stdio.h>
#include <stdlib.h>

    /** Multiplicative and Additive Operators
    +   (Addition)
    -   (Subtraction)
    *   (Multiplication)
    /   (Division)
    %   (Modulus)  **/

int main()
{
    int a=23 ,b=45;
    int sum;
    sum=a+b;
    printf("Sum = %d", sum);
    return 0;
}
